#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main (void) {
    int N, M;
    cin >> N >> M;

    vector<vector<int>> ans(N, vector<int> (N));
    for (int i=0; i<N; i++) {
        for (int j=0; j<N; j++) {
            for (int k=0; k<N; k++) {
                if ()
            }
        }
    }
    return 0;
}