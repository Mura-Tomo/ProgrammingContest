#include <bits/stdc++.h>
using namespace std;

int main (void) {
    int N, S;
    cin >> N >> S;
    vector<int> a(N);
    vector<int> b(N);
    for (int i=0; i<N; i++) {
        cin >> a[i] >> b[i];
    }

    
}